# Copy to .env (gitignored) for any server-side tooling. The browser surface
# never reads this file: a hosted model's key is typed into the model pane
# and kept, at most, in that browser's localStorage.
#
# OPENROUTER_API_KEY=      # for scripts that call OpenRouter from Node (none yet)
# GITHUB_TOKEN=            # for scripts that write to a repository (none yet)
