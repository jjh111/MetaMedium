{
  "name": "metamedium-core",
  "version": "0.1.0",
  "description": "Headless engine for MetaMedium: geometric grounding, multi-parse recognition, spatial graph, and the no-modes session engine (see ARCHITECTURE-v6-SESSION-ENGINE.md)",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "license": "MIT",
  "devDependencies": {
    "typescript": "~5.6.2",
    "vitest": "^4.1.8"
  }
}
